<Window x:Class="AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Panel" Height="600" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Carts">
                <DataGrid x:Name="CartsDataGrid" AutoGenerateColumns="True" />
            </TabItem>
            <TabItem Header="Products">
                <DataGrid x:Name="ProductsDataGrid" AutoGenerateColumns="True" />
            </TabItem>
            <TabItem Header="Categories">
                <DataGrid x:Name="CategoriesDataGrid" AutoGenerateColumns="True" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>