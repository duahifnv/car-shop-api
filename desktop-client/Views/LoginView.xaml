<UserControl x:Class="desktop_client.Views.LoginView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               mc:Ignorable="d"
               d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <!-- Вкладки -->
        <TabControl Background="Transparent" BorderBrush="Transparent">
            <!-- Вкладка Регистрации -->
            <TabItem Header="Регистрация">
                <Grid Margin="10">
                    <StackPanel VerticalAlignment="Center">
                        <!-- Поле "Имя пользователя" -->
                        <Label Content="Имя пользователя:"/>
                        <TextBox x:Name="RegisterUsername" Style="{StaticResource {x:Type TextBox}}" Margin="0,5,0,10"/>

                        <!-- Поле "Почта" -->
                        <Label Content="Почта:"/>
                        <TextBox x:Name="RegisterEmail" Style="{StaticResource {x:Type TextBox}}" Margin="0,5,0,10"/>

                        <!-- Поле "Пароль" -->
                        <Label Content="Пароль:"/>
                        <PasswordBox x:Name="RegisterPassword" Style="{StaticResource {x:Type PasswordBox}}" Margin="0,5,0,20"/>

                        <!-- Кнопка "Зарегистрироваться" -->
                        <Button Content="Зарегистрироваться" Style="{StaticResource {x:Type Button}}" Click="RegisterButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Вкладка Авторизации -->
            <TabItem Header="Авторизация">
                <Grid Margin="10">
                    <StackPanel VerticalAlignment="Center">
                        <!-- Поле "Имя пользователя" -->
                        <Label Content="Имя пользователя:"/>
                        <TextBox x:Name="LoginUsername" Style="{StaticResource {x:Type TextBox}}" Margin="0,5,0,10"/>

                        <!-- Поле "Пароль" -->
                        <Label Content="Пароль:"/>
                        <PasswordBox x:Name="LoginPassword" Style="{StaticResource {x:Type PasswordBox}}" Margin="0,5,0,20"/>

                        <!-- Кнопка "Войти" -->
                        <Button Content="Войти" Style="{StaticResource {x:Type Button}}" Click="LoginButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>